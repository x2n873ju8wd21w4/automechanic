# Локальные эмбеддинги bge-m3 (primary для мультиязыка, без лимитов).
# Тяжёлая установка (~2 GB torch + ~2.3 GB весов при первом запуске) —
# ставить на билд-агенте/твоей машине, НЕ в лёгких CI-джобах.
sentence-transformers>=3.0
